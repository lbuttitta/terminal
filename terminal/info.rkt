#lang info

(define collection 'multi)

(define deps '("terminal-doc"
               "terminal-lib"))

(define implies '("terminal-doc"
                  "terminal-lib"))

(define pkg-desc "A Racket library for manipulating the terminal.")

(define pkg-authors '(lbuttitta))

